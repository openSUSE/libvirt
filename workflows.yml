factory-rebuild:
  steps:
    - trigger_services:
        project: Virtualization
        package: libvirt
  filters:
    event: push
    branches:
      only:
        - factory


sle15sp5-rebuild:
  steps:
    - branch_package:
        source_project: home:jfehlig:gitlab-ci
        source_package: libvirt
        target_project: home:jfehlig:gitlab-ci:workflow-builds
    - trigger_services:
        project: home:jfehlig:gitlab-ci:workflow-builds
        package: libvirt
  filters:
    event: push
    branches:
      only:
        - v9.0.0-sle15sp5